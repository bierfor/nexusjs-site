diff --git a/dist/dev-assets.js b/dist/dev-assets.js
index 8e5f53cffc0f3051a51d213214244cb36f2730da..4c48031c5bc7caf93c3dae2a08c6c424e15d9598 100644
--- a/dist/dev-assets.js
+++ b/dist/dev-assets.js
@@ -281,7 +281,7 @@ async function collectNxFiles(dir) {
  * compile sweeps.  A generation counter prevents a stale in-flight build from
  * overwriting a cache that was reset while the build was running.
  */
-export async function buildAggregatedNxStylesheet(appRoot) {
+export async function buildAggregatedNxStylesheet(appRoot, cssEntry) {
     if (aggregatedCssCache !== null)
         return aggregatedCssCache;
     if (aggregatedCssBuildInFlight !== null)
@@ -291,9 +291,16 @@ export async function buildAggregatedNxStylesheet(appRoot) {
     let promise;
     promise = (async () => {
         try {
+            // Build global CSS first (Tailwind / PostCSS / plain CSS). It is prepended
+            // to the aggregated stylesheet so the browser makes a single CSS request.
+            const globalCss = await buildGlobalStylesheet(appRoot, cssEntry);
             const srcDir = join(appRoot, 'src');
             const files = await collectNxFiles(srcDir);
-            const parts = [LAYER_DECL];
+            const parts = [];
+            if (globalCss) {
+                parts.push(globalCss);
+            }
+            parts.push(LAYER_DECL);
             for (const filepath of files) {
                 // Per-file isolation: a single .nx component with a temporary syntax
                 // error (e.g. editor mid-save) must not abort the entire CSS build.
diff --git a/dist/index.js b/dist/index.js
index c8dbbf1754c74a3c8805fafe0262c9d36f0f30b7..b873ae300f51ac49241eacfc3a86832d86c3599d 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -354,9 +354,10 @@ export async function createNexusServer(opts) {
         }
         // ── Aggregated scoped CSS from all .nx files under src/
         // ── Aggregated scoped CSS from all .nx files under src/
+        // ── Unified stylesheet: global CSS (Tailwind/PostCSS) + scoped .nx styles
         if (url.pathname === '/_nexus/styles.css' && method === 'GET') {
             try {
-                const css = await buildAggregatedNxStylesheet(opts.root);
+                const css = await buildAggregatedNxStylesheet(opts.root, opts.cssEntry);
                 const etag = getAggregatedCssETag();
                 // Conditional GET (If-None-Match) — eliminates FOUC on hard refresh
                 // (Cmd+R / Ctrl+F5).  The browser caches the stylesheet and on each
@@ -663,22 +664,21 @@ export async function createNexusServer(opts) {
                     catch (err) {
                         console.error('[Nexus] Server action preload failed:', err);
                     }
-                    // Pre-warm the aggregated CSS cache in dev mode so that the very
-                    // first page load (and Cmd+R with "Disable cache" in DevTools) does
-                    // not stall waiting for CSS compilation.  The build runs in the
-                    // background — listen() resolves immediately while the CSS compiles
-                    // concurrently.  Any errors are swallowed; the first CSS request
-                    // will fall back to a normal on-demand build.
-                    if (dev) {
-                        buildAggregatedNxStylesheet(opts.root).catch(() => { });
-                        // Pre-warm global CSS and update renderOpts so SSR includes the link.
-                        buildGlobalStylesheet(opts.root, opts.cssEntry)
-                            .then((css) => {
-                            if (css !== null) {
-                                renderOpts.assets.styles = ['/_nexus/global.css', '/_nexus/styles.css'];
-                            }
-                        })
-                            .catch(() => { });
+                    // Pre-warm the unified stylesheet so the first request is fast and the
+                    // HTML already references the correct single CSS file.
+                    try {
+                        if (dev) {
+                            // In dev warm in the background to keep server startup snappy;
+                            // dev radar will trigger a reload once the cache is ready.
+                            buildAggregatedNxStylesheet(opts.root, opts.cssEntry).catch(() => { });
+                        }
+                        else {
+                            // In production block briefly — this avoids FOUC on the first hit.
+                            await buildAggregatedNxStylesheet(opts.root, opts.cssEntry);
+                        }
+                    }
+                    catch {
+                        /* non-fatal: the next request will rebuild on demand */
                     }
                     server.listen(port, () => resolve());
                 })().catch(reject);
@@ -693,17 +693,10 @@ export async function createNexusServer(opts) {
             if (dev) {
                 await preloadRegisteredServerActions(opts.root, true);
                 refreshShieldAllowlist(opts.root, true);
-                // Re-evaluate whether a global CSS entry appeared/disappeared so SSR
-                // links stay in sync with the filesystem.
-                try {
-                    const hasGlobal = (await buildGlobalStylesheet(opts.root, opts.cssEntry)) !== null;
-                    renderOpts.assets.styles = hasGlobal
-                        ? ['/_nexus/global.css', '/_nexus/styles.css']
-                        : ['/_nexus/styles.css'];
-                }
-                catch {
-                    renderOpts.assets.styles = ['/_nexus/styles.css'];
-                }
+                // The unified stylesheet always renders with a single link; no need to
+                // toggle between global.css and styles.css because buildAggregatedNxStylesheet
+                // now prepends the global CSS automatically.
+                renderOpts.assets.styles = ['/_nexus/styles.css'];
                 // Pre-warm the aggregated CSS cache BEFORE telling the browser to reload.
                 // Without this, the sequence is:
                 //   1. bustAggregatedStylesCache() empties the cache
@@ -716,7 +709,7 @@ export async function createNexusServer(opts) {
                 // reloads.  Step 4 becomes an instant cache-hit → styles apply before
                 // first paint, eliminating the flash of unstyled content.
                 try {
-                    await buildAggregatedNxStylesheet(opts.root);
+                    await buildAggregatedNxStylesheet(opts.root, opts.cssEntry);
                 }
                 catch {
                     // CSS build failures are non-fatal — the browser will just get
