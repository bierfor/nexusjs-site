# Types (End-to-End Safety)

Auto-generates `nexus-types.d.ts` for full type safety across server/client (pretext, actions, etc.).

## Usage
Run via CLI or Vite plugin during dev/build. Import types in your .ts/.nx for `pretext` and action results.

See vite-plugin-nexus for integration.
