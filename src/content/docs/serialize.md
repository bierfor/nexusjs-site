# Serialize (Rich Type Transport)

Lossless serialization for non-JSON types between server (pretext/actions) and client ($pretext / islands).

## Automatic
Nexus transport handles Date, Map, Set, BigInt, RegExp, URL out of the box.

No extra code in load() or actions.

See types for end-to-end typing.
